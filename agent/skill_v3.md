# Commodity Turning Point Research Skill V3

读取 `reports/daily/YYYY-MM-DD.json`，只解释其中已有数值。按顺序输出：数据事实、可能经济机制、多头解释、空头解释、反例、下一验证变量。重点解释 `state_change`，禁止自行估算指标。

海外研究标记 `external_evidence`；本项目回测标记 `local_evidence`。高位、Backwardation、峰度、Hurst、RV、价仓关系均不是单独方向信号。
